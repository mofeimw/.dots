# exec /usr/bin/ratpoison
#
# | | | | | | | | | | | |
#
# # /usr/share/xsessions/ratpoison.desktop # #
# [Desktop Entry]
# Name=ratpoison
# Comment=start ratpoison wm
# Exec=/usr/bin/ratpoison
# Type=Application

# remove evrouter PID file
if [ -f /tmp/.evrouter:1 ]; then rm /tmp/.evrouter:1; fi

# start xmodmap
# remap macbook keyboard
(sleep 2 && xmodmap $HOME/.Xmodmap) &

# start evrouter
# enable macbook fn key -> ctrl
(sleep 2 && evrouter /dev/input/event7) &
